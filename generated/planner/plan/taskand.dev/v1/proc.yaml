uri: proc://taskand.dev/planner/plan/v1
interface:
  stdin: { task: string }
  stdout: { valid: boolean, status: string, blueprint?: object, reason?: string }
  exit: { 0: ok, 1: fail-closed, 2: kontrakt }
runtimes: [{ kind: node, bin: bin.mjs }]
