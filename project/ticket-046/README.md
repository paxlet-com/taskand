# Ticket 046: Update project evolution metrics, planfile ticket index, and analysis maps

- **ID**: ticket-046
- **Owner**: antigravity
- **Status**: IN_PROGRESS
- **Workflow state**: EDIT
- **Created**: 2026-09-22

## Goal and scope

Synchronize project evolution metrics, planfile ticket mappings, duplication analysis, and
analysis maps generated by the project toolchain.

## Acceptance criteria

- [x] AC-01: Synchronize evolution analysis maps (`analysis.toon.yaml`, `duplication.toon.yaml`, `evolution.toon.yaml`, `project.toon.yaml`).
- [x] AC-02: Synchronize planfile ticket mappings and export artifacts (`planfile-tickets.yaml`, `mermaid.export`, `context.md`, `index.html`, `prompt.txt`).
- [x] AC-03: Governance checks pass cleanly via `./project/governance-check.sh`.

## Tracking boundary

This directory contains the minimal reviewed intent. Optional participant prose
and raw command logs are not required delivery output.
