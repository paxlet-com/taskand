[project]
name = "taskand-mcp-catalog"
version = "0.1.0"
description = "Native Taskand URI packages projected from local MCP servers"
requires-python = ">=3.11"
dependencies = ["mcp==2.2.0", "jsonschema==4.26.0"]

[tool.uv]
package = false
